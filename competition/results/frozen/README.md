# Frozen evidence

This directory contains only checked-in evidence indexes and metrics that are
safe to publish. Run logs, model responses, credentials, and machine-bound
artifacts stay outside the repository. A `null` metric is intentionally
unfrozen and must not be presented as a measured result.
